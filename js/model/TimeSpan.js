/**
 * 函数对象
 * @param showText      显示的字段
 * @param searchText    搜索的字段
 * @constructor
 */
export default function TimeSpan(showText, searchText) {
    this.showText = showText;
    this.searchText = searchText;
}